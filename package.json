{
  "name": "mockup-creator-pro",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/mockup-studio dev",
    "start": "pnpm run dev",
    "build": "pnpm --filter @workspace/mockup-studio build",
    "typecheck": "pnpm --filter @workspace/mockup-studio typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
